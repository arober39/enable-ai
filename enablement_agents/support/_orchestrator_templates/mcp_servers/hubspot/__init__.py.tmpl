"""Generated minimal MCP server stub for HubSpot.

HubSpot has no vendor-maintained or mature community MCP server at build
time (per mcp_registry/hubspot.yaml). This package provides a minimal
in-process server exposing the three read paths the support orchestrator
needs:

  - get_contact_by_email
  - get_subscription_status
  - get_recent_tickets

Write paths (notes, lifecycle stage updates) are out of scope for v1.
"""
